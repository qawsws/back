Eclipse 환경 설정 파일 제외 (workspace 관련 정보는 공유하지 않음),
.metadata/

Java 빌드 출력물 제외 (클래스 파일 등),
/target/
/build/

Tomcat bin 폴더는 제외하지 않음 (직접 복사해서 포함할 예정이므로),
bin/ ← 이 줄 삭제!,
IDE 설정 파일 제외 (선택사항),
.classpath
.project
.settings/